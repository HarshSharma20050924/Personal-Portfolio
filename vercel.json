{
  "version": 2,
  "installCommand": "npm install",
  "builds": [
    {
      "src": "server/src/server.mjs",
      "use": "@vercel/node",
      "config": {
        "includeFiles": [
          "server/prisma/**"
        ]
      }
    },
    {
      "src": "admin/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist"
      }
    },
    {
      "src": "client/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist"
      }
    }
  ],
  "routes": [
    {
      "handle": "filesystem"
    },
    {
      "src": "/api/(.*)",
      "dest": "/server/src/server.mjs"
    },
    {
      "src": "/admin(/.*)?",
      "dest": "/admin/index.html"
    },
    {
      "src": "/(.*)",
      "dest": "/client/index.html"
    }
  ]
}